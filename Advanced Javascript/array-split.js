const numbers = [1, 2, 3, 4, 5, 6, 7, 8];
// const part = numbers.slice(0, 5);//divide
const removed = numbers.splice(2, 5)//2 position theke suru korbe and porer joyta delete number deya thakbe toyta delete korbe;

// console.log(removed)
// console.log(numbers)
const together = numbers.join("m ")
console.log(together)
