"use strict";
const price = 500;
const freind = 'Murshedul';
const IsStopped = false;
function add(first, second) {
    const result = first + second;
    return result;
}
add(45, 45);
function doubleConsole(number) {
    console.log(2 * number);
}
doubleConsole(5555);
